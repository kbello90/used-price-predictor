# Core Requirements
scikit-learn==1.3.0        # For LinearRegression and preprocessing (StandardScaler, etc.)
numpy==1.24.3              # Numerical operations
pandas==2.0.3              # Data handling
joblib==1.2.0              # Save/load the trained model and scaler
streamlit==1.25.0          # Deployment (no need for Flask/FastAPI)